<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javapandeng.mapper.SeckillMapper">
<!--    目的：为DAO接口的方法提供sql语句-->
    <update id="reduceNumber">
        update
            seckill
        set
            number = number -1
        where seckill_id = #{seckillId}
        and start_time &lt;= #{killTime}
        and end_time >= #{killTime}
        and number > 0;
    </update>
    <select id="queryById2" resultType="Seckill" parameterType="long">
        select  seckill_id ,name ,number ,start_time ,end_time,create_time,price
        from seckill
        where seckill_id = #{seckillId}
    </select >
    <select id= "queryAll" resultType="Seckill">
        select  seckill_id ,name ,number ,start_time ,end_time,create_time,price
        from seckill
        order by create_time desc
        limit #{offset},#{limit}
    </select>
<!--    使用mybatis调用存储过程-->
    <select id="killByProcedure" statementType="CALLABLE">
        call execute_seckill(
        #{seckillId,jdbcType=BIGINT,mode=IN},
        #{phone,jdbcType=BIGINT,mode=IN},
        #{killTime,jdbcType=TIMESTAMP,mode=IN},
        #{result,jdbcType=INTEGER,mode=OUT}
        );
    </select>
    <resultMap id="ResultMapScDto" type="com.javapandeng.po.Seckill">
        <result property="seckillId" column="seckill_id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="INTEGER"/>
        <result property="number" column="number" jdbcType="INTEGER"/>
        <result property="startTime" column="start_time" jdbcType="INTEGER"/>
        <result property="endTime" column="end_time" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="INTEGER"/>
        <result property="price" column="price" jdbcType="INTEGER"/>
        <association property="item" column="seckill_id" select="com.javapandeng.mapper.ItemMapper.load"/>
    </resultMap>

    <!-- 查询列表返回实体-->
    <select id="listBySqlReturnEntity" resultMap="ResultMapScDto">
        ${sql}
    </select>

    <!-- 根据实体主键删除一个实体-->
    <delete id="deleteById" parameterType="java.lang.Long">
        delete from seckill where seckill_id=#{seckillId}
    </delete>

    <insert id="insert" parameterType="com.javapandeng.po.Seckill" >
        insert into seckill(
      seckill_id,name,number ,start_time,end_time,create_time,price
        ) values(
         #{seckillId}, #{name}, #{number}, #{startTime}, #{seckillId}, #{endTime}, #{createTime}, #{price},
        )
    </insert>


</mapper>